<h1>Events#new</h1>

<p>Create new Event</p>


<style>
.field_with_errors {
  padding: 2px;
  background-color: red;
  display: table;
}

</style>

<% if @event.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@event.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
      <% @event.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>




<%= form_for @event do |f| %>

<%= f.label :event_name, "Event Name:" %>
<%= f.text_field :event_name %><br>


<%= f.label :event_date, "Event Date:" %>
<%= f.datetime_field :event_date %><br>

<%= f.label :event_location, "Event Location:" %>
<%= f.text_field :event_location %><br>

<%= f.label :event_text, "Event Text:" %>
<%= f.text_area :event_text %><br>

<%= f.submit "Create Event" %>
<% end %>


<%# Table Event {
  "id" varchar
  "event_name" string
  "event_date" datetime
  "event_location" string
  "event_text" text
  "created_at" datetime
  "updated_at" datetime
} %>